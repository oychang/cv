\ProvidesFile{resume}

\usepackage{hyperref}
\hypersetup{
pdfauthor={\name},
pdftitle={\name: R\'esum\'e},
pdfsubject={R\'esum\'e},
pdfpagemode = UseNone
}

\usepackage[margin=0.5in,top=0.7in,bottom=0.7in]{geometry}

% assuming compilation with lualatex (or equivalent)
\usepackage{fontspec}
\setmainfont[Path=/home/oychang/.fonts/Garamond/,
BoldFont=*-Bd,
ItalicFont=*-It,
Ligatures=TeX,
Renderer=Basic]{GaramondPremrPro}

% set up page headers
\usepackage{fancyhdr} \pagestyle{fancy} \fancyhf{}
\renewcommand{\headrulewidth}{0pt}

% get specific section formatting
\usepackage[compact]{titlesec}
\titleformat{\section}[hang]{}{}{0em}{\rmfamily\mdseries\LARGE\textbf}[]
\titleformat{\subsection}[hang]{}{}{0em}{\rmfamily\mdseries\itshape\large}[]

% just doublecheck that this is correct
\usepackage{setspace} \singlespacing

% compact itemizes
\usepackage{enumitem}
\setitemize{noitemsep,topsep=0pt,parsep=0pt,partopsep=0pt}

% no indents
\setlength\parindent{0em}
